<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="DoradSmartphone.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:DoradSmartphone"
    xmlns:views ="clr-namespace:DoradSmartphone.Views"
    Shell.NavBarIsVisible="False">

    <ShellContent
                Title="Home"
                ContentTemplate="{DataTemplate views:LoadingPage}"
                Route="LoadingPage" />

    <TabBar >
        <Tab Title="Routes" Icon="route.png">
            <ShellContent
                Title="Routes"
                ContentTemplate="{DataTemplate views:ExercisePage}"
                Route="ExercisePage" />
        </Tab>
        <Tab Title="Stats" Icon="barchart.png">
            <ShellContent
                Title="Home"
                ContentTemplate="{DataTemplate local:MainPage}"
                Route="MainPage" />
        </Tab>
        <Tab Title="Widget" Icon="dashboard.png">
            <ShellContent
                Title="Home"
                ContentTemplate="{DataTemplate local:MainPage}"
                Route="MainPage" />
        </Tab>
        <Tab Title="Avatar" Icon="runner">
            <ShellContent
                Title="Home"
                ContentTemplate="{DataTemplate local:MainPage}"
                Route="MainPage" />
        </Tab>
    </TabBar>
</Shell>
